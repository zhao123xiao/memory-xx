# projection

Frozen path for Markdown projection and exporter code.

Phase B3 adds the projection foundation:

- frozen view constants and job/state/error models
- stable ID / stable sort / visibility mapping
- writer utilities for frontmatter, markdown rendering, diff guard, and atomic write
- minimal templates for the seven canonical projection views
